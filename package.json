{
  "name": "koc-design-system",
  "version": "0.1.0",
  "private": true,
  "description": "KOC Design System \u2014 unified React + shadcn foundation for Kuwait Oil Company digital products",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build:tokens": "npm run build --workspace=@koc/tokens",
    "test:tokens": "npm run test --workspace=@koc/tokens",
    "check:drift": "tsx packages/tokens/src/check-drift.ts apps/docs/src/styles.css",
    "check:motion": "tsx packages/tokens/src/check-motion.ts packages/ui/src apps/docs/src",
    "check:parity": "tsx apps/docs/scripts/check-parity.ts",
    "test": "npm run test:tokens && npm run check:drift && npm run check:motion && npm run registry && npm run check:parity && npm run test:a11y",
    "dev": "npm run dev --workspace=@koc/docs",
    "dev:clean": "rm -rf apps/docs/node_modules/.vite && npm run dev",
    "build": "npm run build:tokens && npm run check:drift && npm run check:motion && npm run registry && npm run check:parity && npm run build --workspace=@koc/docs",
    "registry": "npm run build:registry --workspace=@koc/docs",
    "test:a11y": "npm run test:a11y --workspace=@koc/docs"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "tsx": "^4.19.2"
  }
}
